$TTL 3D

@	SOA	dns.{{zone}}	root.{{zone}}.	(12 4h 1h 1w 1h)

@	IN	NS	dns.{{zone}}.

dns.{{zone}}.	IN	A	{{pillar['zones'][zone]['dns_server']}}

{% for dns_entry in pillar['zones'][zone]['entries'] %}
{{dns_entry}}	IN	A	{{pillar['zones'][zone]['entries'][dns_entry]}}
{% endfor %}
